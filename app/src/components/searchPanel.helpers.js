/**
 * searchPanel.helpers — STUB. Plan 03-05 Task 1 RED commit.
 * Real implementation lands in the GREEN commit.
 */

export function isSearchHotkey(_event) {
  return false;
}

export function buildSnippet(_body, _query, _maxLen) {
  return [];
}

export function formatBreadcrumb(_prd_id, _level) {
  return '';
}
